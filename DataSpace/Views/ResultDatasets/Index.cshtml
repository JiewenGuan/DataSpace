@model IEnumerable<DataSpace.Models.ResultDataset>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RepoUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Experiment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RepoUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Experiment.Title)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DatasetID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DatasetID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DatasetID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
